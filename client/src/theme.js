import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  styles: {
    global: {
      body: {
        bg: "#9379c2", // Or any color you prefer
      },
    },
  },
});

export default theme;
